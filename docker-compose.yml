services:
  musique-db:
    image: postgres:16-alpine
    container_name: musique-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-musique}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - musique_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-musique}']
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  musique_postgres_data:
